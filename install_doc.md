to setup backend:



cmds:

npm init - initial setup for create **package.json**

npm install express - **install express**

npm install --save-dev nodemon/ npm install nodemon- **install nodemon**

npm install dotenv- **install the env to setup the environment variables**

npm install mongoose- **install mongoose to connect the database**

npm install bcryptjs- **install bcrypt to encrypt and decrypt the password with hash**

npm i jsonwebtoken- **install jsonwebtoken to create token with secret key**

atlas connection- **mongodb+srv://quickchat:WBPB8HuTWJOAFxtd@cluster0.xdrh2qy.mongodb.net/quick-chat**


project structure:

dbconfig- under config folder- **connect with DB**

models - **create collections(models) with schema**

Controllers- **for routing**